import { Button } from "@mui/material";

function Btn() {
  return (
    <Button variant="outlined" type="submit" sx={{ margin: "20px 5px" }}>
      Iniciar sessión
    </Button>
  );
}

export default Btn;
